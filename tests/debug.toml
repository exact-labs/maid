import = ["tests/import.toml", "tests/import.yml"]

[env]
STRING = "hello world"

[project]
name = "debug"
version = "0.0.1"

[project.server]
address = { ip = "localhost", port = 3500, ssl = false }
token = "ZWZ0Z3loZHVpandzZGhneXVpb2w="

[tasks.test]
script = "echo %{env.STRING}"
depends = ["other"]
remote = {push = ["src", "cargo.toml"], pull = "maid", dependencies = "rust"}